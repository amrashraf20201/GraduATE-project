package com.example.explurerhub.Repository;

import com.example.explurerhub.Model.CairoMusiums;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FavouriteMusimRepo extends JpaRepository<CairoMusiums,Long> {
}
